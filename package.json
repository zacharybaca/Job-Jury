{
  "name": "job-jury",
  "version": "1.0.0",
  "description": "A platform for unbiased workplace reviews, empowering employees with transparency.",
  "main": "server/server.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix client && npm install --prefix server",
    "clean-install": "rimraf node_modules client/node_modules server/node_modules **/package-lock.json && npm run install-all",
    "server": "nodemon --prefix server server.js",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run client\" \"npm run server\"",
    "start": "node server/server.js",
    "format": "prettier --write \"{client/src,server}/**/*.{js,jsx,json,css,md}\""
  },
  "author": "Zachary Baca",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0",
    "prettier": "^3.8.1",
    "rimraf": "^6.1.3"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zacharybaca/job-jury.git"
  }
}
